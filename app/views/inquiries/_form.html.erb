<%= form_with(model: inquiry, local: true) do |form| %>
  <% if inquiry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inquiry.errors.count, "error") %> prohibited this inquiry from being saved:</h2>

      <ul>
      <% inquiry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hunter_id %>
    <%= form.text_field :hunter_id %>
  </div>

  <div class="field">
    <%= form.label :house_belongs_to %>
    <%= form.text_field :house_belongs_to %>
  </div>

  <div class="field">
    <%= form.label :query %>
    <%= form.text_field :query %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
